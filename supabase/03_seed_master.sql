-- ============================================================
-- 03_seed_master.sql — 初期話題マスタ 10件
--
-- 2026-08-24 に実行して成功したもの。02_rls.sql のあとに流す。
--
-- user_id を NULL にするのが肝。02_rls.sql の topic_masters のポリシー
--   using (user_id = auth.uid() or user_id is null)
-- により「全員に見える共通の話題」になる。
--
-- NULL は auth.uid() と一致しないので、アプリ側からは共通の話題を作れない。
-- 追加できるのは SQL Editor（管理者権限）だけ。これは意図通り。
--
-- 並びの基準：上ほど誰にでも当てはまり、下ほど当たれば強いが人を選ぶ。
-- sort_order が効くのは新規のお客さんの記録画面だけ。
-- 2回目以降は、その人と実際に話した回数の多い順に置き換わる（§6）。
--
-- ★ この10個から増やさないこと
--   「天気」を入れない  … 検証で35前後。必ず下位に沈み、バブル10個の枠を1つ無駄にする
--   「その他」を入れない … あると全部そこに逃げ、分類として機能しなくなる
-- ============================================================

insert into topic_masters (user_id, name, sort_order) values
  (null, '家族',           1),
  (null, '食べ物',         2),
  (null, '暮らし',         3),
  (null, '仕事',           4),
  (null, '旅行・おでかけ', 5),
  (null, '健康・美容',     6),
  (null, '趣味',           7),
  (null, 'エンタメ',       8),
  (null, 'ペット',         9),
  (null, 'スポーツ',      10);

-- ============================================================
-- 確認（2026-08-24 実施済み）
--   select sort_order, name, user_id from topic_masters order by sort_order;
--     → 10行。user_id はすべて NULL
-- ============================================================
